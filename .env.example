# ───────────────────────────────────────────────────────────
#  環境變數範本 / Environment variable template
# ───────────────────────────────────────────────────────────
#  ⚠️ 這是「範本」檔，只放變數名稱、不要填入真實值。
#  真實的金鑰請設定在 Vercel 環境變數（Project → Settings →
#  Environment Variables），或本地的 .env（已被 .gitignore 忽略）。
#  絕對不要把含有真實值的 .env / .env.* 檔 commit 進這個 public repo。
# ───────────────────────────────────────────────────────────

# AI 小幫手 — Gemini API 金鑰（在伺服器端 api/chat.js 使用，不可加 VITE_ 前綴）
GEMINI_API_KEY_1=
GEMINI_API_KEY_2=
